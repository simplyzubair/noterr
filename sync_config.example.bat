rem Copy this file to sync_config.bat, then fill your Cloudflare Worker URL.
rem sync_config.bat is ignored by git so local values do not get committed.

set "NOTERR_SYNC_URL=https://noterr-sync.YOUR-SUBDOMAIN.workers.dev"
